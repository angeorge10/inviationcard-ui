import { AuthenticationApiConfig } from './authentication-api-config';

describe('LoginApiConfig', () => {
  it('should create an instance', () => {
    expect(new AuthenticationApiConfig()).toBeTruthy();
  });
});
